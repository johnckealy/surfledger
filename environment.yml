name: surfledger

channels:
  - conda-forge

dependencies:
  - django
  - gunicorn
  - pygrib
  - pip
  - pip:
    - djangorestframework
    - psycopg2
    - django-widget-tweaks
    - first
    - selenium
    - python-dotenv
    - pip install wget
